# ── Core ──────────────────────────────────────────────────
flask>=3.0
werkzeug>=3.0
requests>=2.32

# ── Embeddings & Vector DB ────────────────────────────────
sentence-transformers>=2.7
faiss-cpu>=1.8          # CPU FAISS (embeddings run on GPU via sentence-transformers)

# ── Data handling ─────────────────────────────────────────
pandas>=2.2
numpy>=1.26
openpyxl>=3.1           # for .xlsx

# ── Document loaders ──────────────────────────────────────
pdfplumber>=0.11
chardet>=5.2

# ── PyTorch GPU (RTX 3050 — CUDA 12.x) ───────────────────
# Run this command instead of pip installing torch from here:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# (cu121 = CUDA 12.1 — works for RTX 3050 with latest drivers)
# Check your CUDA version with: nvidia-smi

# ── HuggingFace backend (optional) ───────────────────────
transformers>=4.40
accelerate>=0.30
